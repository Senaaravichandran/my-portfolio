Keep replacement images for the `/godmode` glass-scroll page in this folder.

Use files like:

- `water-ripples.jpg`
- `sand-drift.jpg`
- `mist-sky.jpg`

Then update the `image` paths in `src/pages/GodmodePage.jsx` to `/images/godmode/your-file-name.jpg`.
